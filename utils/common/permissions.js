export const saveCurrentPermissions = async (page) => {
    // Stub implementation
    console.log("DEBUG: saveCurrentPermissions called");
};

export const hasPermission = (permission) => {
    // Stub implementation
    return true; // Allow all permissions by default for now
};
