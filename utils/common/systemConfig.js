export const saveSystemConfig = async (page) => {
    // Stub implementation
    console.log("DEBUG: saveSystemConfig called");
};

export const getSystemConfig = (key) => {
    // Stub implementation
    return null;
};
